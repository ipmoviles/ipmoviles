---
import { changeLanguage, t } from "i18next";
import Layout from "../../layouts/Layout.astro";
import sendGrid from "@sendgrid/mail";
import ContactForm from "../../components/ContactForm.astro";
import Icon from "astro-icon";

changeLanguage("es");

sendGrid.setApiKey(import.meta.env.SENDGRID_API_KEY);
if (Astro.request.method === "POST") {
  try {
    // Getting posted data from our contact form
    const data = await Astro.request.formData();
    const name = data.get("name");
    const email = data.get("email");
    const phone = data.get("phone");
    const message = data.get("message");
    // TODO: Do not forget to do validation and error handling over received data from your contact form.
    // Sending email
    const msg = {
      to: "ipmovilesdev@gmail.com",
      from: "ipmovilesdev@gmail.com",
      replyTo: { email: email, name: name },
      subject: `Contact form submission from ${name}`,
      text: `
Subject: Contacto de ${name}

Hello,

You have received a new inquiry from a potential customer. Here are the details:

Name: ${name}
Email: ${email}
Phone: ${phone}
Message:${message}

Please follow up with them to provide the atention they need. Building strong customer relationships is key to our success.

Best regards,
IP Moviles
`,
    };
    await sendGrid
      .send(msg)
      .then(() => {
        console.log("Email sent");
      })
      .catch((error) => {
        console.error(error);
      });
  } catch (error) {
    console.error(error);
  }
}
---

<Layout title="Contact">
  <div class="container">
    <div class="page-title">
      <h1 class="contact-title">{t("contact_us.contact_us_title")}</h1>
      <p class="contact-intro">{t("contact_us.form.contac_text01")}</p>
    </div>
    <div class="contact-container">
      <div class="section-container">
        <div class="form-container">
          <h2 class="form-title">{t("contact_us.form.title")}</h2>
          <!-- form component -->
          <ContactForm />
        </div>
        <div class="contact-container">
          <h2>{t("contact_us.contact_info.contact_info_title")}</h2>
          <div class="contact-content">
            <h3>{t("contact_us.contact_info.USA_phone")}</h3>
            <p>+57 320 8542698</p>
            <p>+57 313 2723646</p>
          </div>

          <div class="contact-content">
            <h3>{t("contact_us.contact_info.colombian_phone")}</h3>
            <p>+57 320 8542698</p>
            <p>+57 313 2723646</p>
          </div>

          <div class="contact-content">
            <h3>{t("contact_us.contact_info.emails")}</h3>
            <a href="mailto:andres.morales@ipmoviles.net"
              >andres.morales@ipmoviles.net</a
            >
            <a href="mailto:karina.otalora@ipmoviles.net"
              >karina.otalora@ipmoviles.net</a
            >
            <a href="mailto:info@ipmoviles.com">info@ipmoviles.com</a>
          </div>

          <div class="contact-content">
            <h3>{t("contact_us.contact_info.redes")}</h3>
            <div class="icons">
              <a href="https://www.linkedin.com/ipmoviles" target="_blank">
                <Icon name="mdi:linkedin" />
              </a>
              <a href="https://www.instagram.com/ip_moviles/" target="_blank">
                <Icon name="mdi:instagram" />
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="contact-info">
      <div class="mapa">
        <h2 class="title">{t("contact_us.location_title")}</h2>
        <p>{t("contact_us.location_sub")}</p>
        <div>
          <span>
            <h2 class="desktop">{t("contact_us.united_states")}</h2>
            <h2 class="mobile">{t("contact_us.florida")}</h2>
            <iframe
              src="https://www.google.com/maps/embed?pb=!1m17!1m12!1m3!1d1881.5471667048807!2d-80.1448023840359!3d26.18185068006446!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m2!1m1!2zMjbCsDEwJzU0LjkiTiA4MMKwMDgnMzcuNyJX!5e0!3m2!1ses!2sco!4v1686007858948!5m2!1ses!2sco"
              style="border:0;"
              allowfullscreen=""
              loading="lazy"
              referrerpolicy="no-referrer-when-downgrade"></iframe>
            <p>210 NE 45th St, Oakland Park - Florida 33334.</p>
          </span>

          <span>
            <h2 class="desktop">{t("contact_us.colombia_")}</h2>
            <h2 class="mobile">{t("contact_us.colombia")}</h2>
            <iframe
              src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3975.4927983146786!2d-74.04921073045426!3d4.856756492685045!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8e3f87f272775d9b%3A0xa56fbd10bdb4aa8f!2sCra.+1a+%2311-130%2C+Ch%C3%ADa%2C+Cundinamarca!5e0!3m2!1ses!2sco!4v1535412193622"
              frameborder="0"
              style="border:0"
              allowfullscreen=""></iframe>
            <p>Cra. 1a #11-130 Chía - Cundinamarca.</p>
          </span>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style lang="scss">
  @use "../../assets/scss/base/_breakpoint.scss" as *;

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;

    > .page-title {
      display: flex;
      flex-direction: column;
      width: 90%;
      max-width: 1350px;
      gap: 30px;
      margin: 40px 0px;
      > .contact-title {
        font-size: 3.6rem;
        font-weight: 700;

        @include breakpoint(md) {
          font-size: 4.8rem;
        }

        @include breakpoint(lg) {
          font-size: 5.6rem;
        }

        @include breakpoint(xl) {
          font-size: 6.4rem;
        }
      }
    }

    > .contact-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 25px;
      width: 100%;
      margin: 40px 0px;

      > .section-container {
        display: flex;
        flex-direction: column;
        gap: 40px;
        width: 90%;
        max-width: 1350px;

        @include breakpoint(md) {
          flex-direction: row;
          gap: 20px;
        }

        > .form-container {
          display: flex;
          flex-direction: column;
          gap: 25px;
          width: 100%;
          height: 100%;
          @include breakpoint(md) {
            width: 65%;
            height: auto;
            background-color: var(--card-background-color);
            box-sizing: border-box;
            border-radius: var(--radius-default);
            padding: 30px;
            color: var(--color-mint-cream);
          }

          @include breakpoint(lg) {
            width: 70%;
          }

          @include breakpoint(xl) {
            width: 75%;
          }
        }

        > .contact-container {
          display: flex;
          flex-direction: column;
          gap: 10px;
          width: 100%;
          background-color: var(--card-background-color);
          border-radius: var(--radius-default);
          padding: 30px;
          color: var(--color-mint-cream);
          > h2 {
            font-size: 2rem;
          }

          h3 {
            font-size: 1.8rem;
          }

          > .contact-content {
            display: flex;
            flex-direction: column;
            width: 100%;
            p {
              font-size: 1.6rem;
            }
            > a {
              font-size: 1.2rem;
              @include breakpoint(md) {
                font-size: 1.4rem;
              }
            }

            > .icons {
              display: flex;
              gap: 10px;
              [astro-icon] {
                margin-top: 5px;
                width: 40px;
                height: 40px;
                color: var(--action-color);
              }
            }
          }
          @include breakpoint(md) {
            width: 35%;
            background-color: var(--card-background-color);
            box-sizing: border-box;
            border-radius: var(--radius-default);
            padding: 30px;
            color: var(--color-mint-cream);
          }

          @include breakpoint(lg) {
            width: 30%;
          }

          @include breakpoint(xl) {
            width: 25%;
            h2 {
              font-size: 2.2rem;
            }
            h3 {
              font-size: 2rem;
            }
            p {
              font-size: 1.8rem;
            }
          }
        }
      }
    }
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: 20px;
    background-color: var(--card-background-color);
    box-sizing: border-box;
    padding: 30px;
    border-radius: 5px;
    @include breakpoint(md) {
      width: auto;
      height: auto;
    }
  }

  .contact-info h2 {
    font-size: 20px;
    font-style: normal;
    font-weight: 600;
  }

  .phone {
    display: flex;
    flex-direction: column;
    gap: 3px;
    font-size: 16px;
    font-style: normal;
    font-weight: 500;
  }

  .phone p {
    display: flex;
    font-size: 16px;
    font-style: normal;
    font-weight: 500;
  }

  .phone h3 {
    margin-bottom: 4px;
    font-size: 16px;
    font-style: normal;
    font-weight: 700;
  }

  .redes span {
    margin-top: 12px;
    display: flex;
    gap: 15px;
  }

  .desktop {
    display: none;
    box-sizing: border-box;
    margin-top: 30px;
    @include breakpoint(md) {
      margin-top: 0;
      display: flex;
      text-align: center;
      color: var(--action-color);
    }
  }

  .mobile {
    display: flex;
    box-sizing: border-box;
    padding: 14px 0;
    text-align: left;
    margin-top: 30px;
    @include breakpoint(md) {
      display: none;
    }
  }

  iframe {
    width: 100%;
    @include breakpoint(md) {
      width: 510px;
      height: 300px;
    }
  }

  .mapa {
    @include breakpoint(md) {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-top: 80px;
      width: 100%;
    }
  }

  .mapa .title {
    display: none;
    @include breakpoint(md) {
      display: flex;
    }
  }

  .mapa div {
    @include breakpoint(md) {
      display: grid;
      grid-template-columns: 1fr 1fr;
      justify-items: center;
      grid-gap: 20px;
    }
  }

  .mapa span {
    @include breakpoint(md) {
      display: flex;
      flex-direction: column;
      font-size: 20px;
      align-items: center;
      gap: 20px;
    }
  }

  .mapa span p {
    @include breakpoint(md) {
      font-size: 20px;
    }
  }
</style>
